---
data: 25-09-2026
projeto: JurisLeo
branch: fase-2 (commit b39bae9)
tipo: revisão de código (nada corrigido ainda)
---

# Revisão de código — 25-09-2026

Três revisores em paralelo: serviços/regras/consola, hooks + páginas A–F, páginas G–Z + componentes. Estado de partida: 547 testes a passar, lint com 1 aviso, build a passar (chunk principal 1,07 MB, precache 1,7 MB).

Legenda: 🔴 crítico · 🟠 alto · 🟡 médio · ⚪ baixo · **[regra]** = mexe em regras de negócio, confirmar com a Leonor antes.

## 🔴 Críticos — perda de dados ou ecrã partido

1. **Consola rebenta sem cadeiras aprovadas** — `Consola.jsx:218`. `resumo.media.media.toFixed(2)` com `media: null` → TypeError, ecrã branco. É o caso de hoje (setembro, nada fechado). Correção: `resumo.media?.media != null` + teste.
2. **"Repor cadeiras do 2.º ano" apaga notas, faltas e pesos** — `Definicoes.jsx:77-85` + `initFirestore.js:20,32,39`. `setDoc` sem `merge`, sem confirmação, botão visível para ela. Correção: tirar da UI dela ou confirmar, e semear só com `merge` quando o documento não existe.
3. **Caderno perde o texto** — `EditorCaderno.jsx:73-78` + `Anotacao.jsx:94-95`. Sem gravação automática nem aviso ao sair; "Guardar" sem título não faz nada em silêncio. Correção: autosave com atraso (~1,5 s) + `pagehide`, "Sem título" por omissão, `useBlocker` com alterações pendentes.

## 🟠 Altos

4. **Faltas offline ficam a meio** — `registoFaltas.js:33-62`. Com cache persistente, `await setDoc` espera pelo servidor; sem rede grava `estadosAula` mas não `faltasRegisto` nem a contagem. Correção: `writeBatch` sem esperar, ou contagem derivada dos snapshots.
5. **Botões presos em "A guardar..." sem rede** — `Glossario:80`, `Leituras:47`, `MapaMental:45`, `TopicosCorrecao:42`, `ModoExame:199`, `Tarefas:387-397`, `Onboarding:48`, `Notas:64`, `CartaoBemEstar:34`, `Anotacao:93`, `Caso:67`, `Artigos:98`, `Flashcards:98`, `Estudo:85` (aqui perde a sessão). Correção: não esperar pela confirmação do servidor, `try/finally`.
6. **Flashcards saltam cartões** — `Flashcards.jsx:28-30,163-176`. A fila recalcula-se ao vivo; cada resposta tira o cartão e `indice+1` salta o seguinte (10 prontos → vê 5; no fim fica preso). Correção: congelar os ids ao abrir a sessão.
7. **Cronómetro perde tempo no iPhone** — `useCronometro.js:13`. `setInterval` +1 s parado com ecrã bloqueado. Correção: calcular a partir de marcas de tempo.
8. **Dias até à frequência diferentes em cada ecrã** — `frequencia.js:27-30` (usado no Dashboard) conta blocos de 24 h; Frequência e Consola usam `diasEntre`. Dashboard ignora `cancelado` e exames; o anel enche ao contrário (`Dashboard.jsx:89-92`). Correção: usar `proximaProva`/`diasEntre` e a mesma fórmula do `Anel`.
9. **Editar evento de vários dias muda o início** — `useCalendario.js:23` + `Calendario.jsx:273`. Correção: guardar `dataOriginal` em cada cópia.
10. **Método B nunca chega a "aprovada"** — `avaliacao.js:117-128` + `notas.js:58-72`. AC ≤ 9 e depois 15 no escrito continua `passaMetodoB`. **[regra]**
11. **Aulas práticas previstas = 30 em todas** — `dadosLeonor.js:26…78`. O próprio gerador dá DF 28, HRI 28, DO 29, DA 29, DIP 30 → margem de faltas a mais. **[regra]**
12. **Tema claro ilegível** — `Tarefas.css` (títulos e texto brancos) e `Calendario.css` (185 cores fixas, escrito só para escuro).
13. **Onboarding apaga `anoLetivo`** e não guarda cadeiras desmarcadas nem notas anteriores — `Onboarding.jsx:48-57`.
14. **Leituras sem total de páginas ficam presas a 0** — `Leituras.jsx:77`; o input grava a cada tecla (`:111-116`).
15. **Nada usa `safe-area-inset-top`** — com `black-translucent`, o topo de todas as páginas fica debaixo da Dynamic Island na app instalada.

## 🔐 Segurança e privacidade

16. 🟠 **A consola descarrega o texto dela** — `useConsola.js:9,36-37` lê `anotacoes` e `casos` completos, que ficam no IndexedDB do aparelho do Vini; as regras deixam o admin ler tudo. A promessa "só números" existe só na UI. Correção: documento `resumoConsola/dados` só com contagens, regras restritas a isso, `memoryLocalCache` na consola.
17. 🟡 **Bem-estar desligado continua visível ao Vini** — a consola lê `registosDiarios` sempre.
18. 🟡 **Registo público aberto** — qualquer pessoa cria conta e escreve no projeto. Correção: desligar registo no Auth ou limitar por uid nas regras.
19. ⚪ PIN só no cliente (é um trinco de ecrã, assumido); email do Vini e uid dela no bundle; `firebase.json` sem cabeçalhos de segurança; `Significado.jsx:42` abre `dados.url` sem validar `https:`.
- Sem XSS: não há `dangerouslySetInnerHTML`, o tiptap guarda JSON, LanguageTool só depois do aviso de privacidade.

## 🟡 Médios

- Simulador de notas errado em 3 casos (`avaliacao.js:163-192`) **[regra]**; margem de injustificadas usa previstas, não lecionadas (`faltas.js:75-79`) **[regra]**; `prazoComprovativo` ignora feriados (`faltas.js:135-143`).
- `ThemeContext.jsx:26-43`: a flag anti-ciclo engole a primeira mudança de tema da sessão; o tema não segue `prefers-color-scheme` e a semente força `dark`.
- Flashcards ficam prontos só à hora exata da última revisão (`repeticaoEspacada.js:35-40`).
- Backup "de tudo" deixa 10 coleções de fora e os Timestamps saem crus (`exportar.js`).
- Faltas: marcar "faltei" no Dashboard e depois no ecrã Faltas conta duas vezes (`Faltas.jsx:66,74`).
- Martelo de juiz nunca se vê (`Tarefas.jsx:246-253`); editar tarefa concluída não faz nada (`:340`).
- Folhas fecham ao tocar fora e perdem o que estava escrito (`Tarefas:404`, `ModalCriarEvento:204`, `FormAulaHorario:62`); evento + lembretes sem `writeBatch`.
- "Desfazer" da revisão de texto apaga o que ela escreveu depois; `Toast` nunca fecha em `Faltas`/`Fichas`; `AcoesAula` remonta o formulário do sumário e perde texto.
- Modo Exame: relógio só em memória, "Terminar" sem confirmação. Mapa mental: setas apagam-se ao primeiro toque.
- `CartaoBemEstar.jsx:35` apaga o registo da manhã no cálculo da noite; `CartaoBalancoDomingo` abre 4 listeners todos os dias.
- Hooks sem callback de erro no `onSnapshot` → "A carregar" eterno sem sessão ou com regra recusada.
- Login sem `<form>` nem `autocomplete` (Palavras-passe do iCloud não preenchem).
- iPhone: `:hover` sem `@media (hover: hover)` (0 de ~60), alvos de toque < 44 px, folhas em `vh`, inputs < 16 px (só não fazem zoom porque o zoom está desligado para tudo).
- Acessibilidade: botões só com ícone sem `aria-label`, `div onClick`, modais sem Escape nem foco preso, contraste fraco nos dourados sobre claro.

## Violações do CLAUDE.md

- ~1000 cores fixas fora do `index.css` (Calendario 185, Tarefas 93, ModalCriarEvento 84…); `'#b8963e'` repetido 23 vezes (nem é o `--gold`).
- Tipografia: Playfair (74×), Lato (43×), Montserrat (30×, nunca carregada), importadas em `Dashboard.css:2`.
- ~17 páginas com classe `.dark` própria em vez de variáveis.
- Firestore dentro de páginas (`Calendario`, `Definicoes`, `Tarefas`, `Onboarding`, `ModalCriarEvento`).
- Regras sem fonte: nenhuma regra de `avaliacao.js` cita o regulamento; "5 dias para o recurso" escrito em `Notas.jsx:70`; `aulasPraticasPrevistas || 30` repetido 7 vezes na UI. **[regra]**
- `CLAUDE.md` desatualizado (diz que não há `.env.example`; lista de ficheiros de 17-09).

## Duplicação e código morto

- 17 hooks de lista e 4 de item com o mesmo esqueleto → `useColecao` / `useDocumento`. 89 cópias de `getAuth().currentUser?.uid`.
- 9 cópias de `paraData()` em `services/` (e **não existe** em `datas.js`) + 8 conversões à mão.
- Folha de baixo copiada 7 vezes → um `<Folha>` resolve dvh, zona segura, Escape e foco.
- "Dispensar até X" 4 vezes → `useDispensadoAte`.
- Exports mortos: `choquesPorDia`, `calcularProximaRevisao`, `contaComoFalta`, `temLacunas`, `diasUteisEntre`, `cabeNoDia`, `pararVoz` (devia ser chamado), `nomesCadeiras`, etc. `gsap` e `animejs` instalados e nunca importados.

## Desempenho

- 38 páginas importadas de forma estática → `React.lazy` por rota. Se o chunk passar os 2 MiB, sai do precache e a app deixa de abrir offline.

## Ordem sugerida

1. Críticos 1–3 (consola, repor cadeiras, autosave do caderno).
2. Escritas offline (4–5) e flashcards (6), cronómetro (7).
3. Dias até à frequência num só motor (8) — entra no redesign do Dashboard.
4. `--zona-segura-topo`, tema claro de Tarefas/Calendário — entra no redesign.
5. Privacidade da consola (16–18) — mexe em regras, sessão própria.
6. Regras de negócio marcadas **[regra]** — só depois de a Leonor confirmar.
